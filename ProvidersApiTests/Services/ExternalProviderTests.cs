namespace ProvidersApiTests.Services
{
    class ExternalProviderTests
    {
        // I think httpClient has socket exhaustion issues, so there is an interface one can DI I think.
        // Would rather have a nice interface to mock out so that one can check what the interface is passed when a call is made.
    }
}
